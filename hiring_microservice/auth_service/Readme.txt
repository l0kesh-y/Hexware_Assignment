getdb()

It manages DB session
It creates session factory
It provides DB dependency
It belongs to database layer

To run the application:

uvicorn app.main:app --reload --port 8001

To check the docker installed
docker --version

To build the docker
docker-compose up --build

not working
which docker compose

docker ps = show active containers

docker images = show build images

